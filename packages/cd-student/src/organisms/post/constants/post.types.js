const TEXT = "text";
const IMAGE = "image";
const VIDEO = "video";
const POLL = "poll";
const ANNOUNCEMENT = "announcement";

const POST_TYPES = {
  TEXT,
  IMAGE,
  VIDEO,
  POLL,
  ANNOUNCEMENT
};

export default POST_TYPES;
